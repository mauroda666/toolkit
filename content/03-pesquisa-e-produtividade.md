# 🎓 Pipelines de valor com os 50 sites (@mertmetindev)

Organizei os 50 sites em fluxos de trabalho prontos por objetivo.

## Pesquisa acadêmica 100% grátis
`semanticscholar.org` (busca) → `connectedpapers.com` (mapa de citações) → `sci-hub.se`/`libgen.is` (texto completo) → `scispace.com`/`elicit.org` (entender e extrair) → `consensus.app` (consenso) → `summarize.tech` (vídeos).

## Kit de design sem custo
`photopea.com` (Photoshop) + `remove.bg`/`cleanup.pictures`/`unscreen.com` (limpeza) + `shots.so`/`smartmockups.com` (mockups) + `squoosh.app` (compressão). **Monetizável:** thumbnails, mockups e artes sob demanda.

## Cinto de utilidades do dev
`regex101.com` · `jsonformatter.org` · `codebeautify.org` · `explainshell.com` · `carbon.now.sh`/`ray.so` (código como imagem) · `phind.com` (busca IA).

## Segurança & privacidade
`haveibeenpwned.com` · `virustotal.com` · `temp-mail.org`/`10minutemail.com` · `privnote.com` · `archive.ph` (preservar prova).

## Foco / rotina
`musicforprogramming.net` · `mynoise.net` · `coffitivity.com` · `raindrop.io` (bookmarks).

> Filtre todos por valor no dashboard (`web/index.html`).
